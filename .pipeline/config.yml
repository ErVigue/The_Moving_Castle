 
steps:
  mtaBuild:
    buildTarget: 'CF'
    applicationName: 'Kakashi'
    verbose: true
  cloudFoundryDeploy:
    cloudFoundry:
      credentialsId: 'CF'
      apiEndpoint: '<CF Endpoint>'
      org: '<CF Organization>'
      space: '<CF Space>'
